# Шаблон документа решения

Один документ — одно проектное решение. Документ **не содержит** принятого решения: он ведёт к нему и задаёт форму записи. Принятое решение живёт в ADR конкретного проекта и ссылается сюда.

## Когда заводить документ

Отбор по двум осям — цене изменения и влиянию:

| | Дёшево изменить | Дорого изменить |
|---|---|---|
| **Высокое влияние** | Короткая форма: разделы 1, 2, 5 | **Полная форма: все разделы** |
| **Низкое влияние** | Не документируем | Обычно значит, что решение можно отложить |

Влияние считается по двум признакам: видно ли пользователю и видно ли в счёте. Обратимое решение с большим влиянием на экономику (класс хранения, стратегия кэша, размер пула) заслуживает короткой формы — оси и эвристики без рыночного слоя.

## Правила заполнения

**Факт, а не оценка.** В матрице пишется измеримое утверждение или «нет данных». Балл «4 из 5» скрывает, откуда он взялся, и позволяет подогнать итог под уже выбранный вариант.

**Источник факта помечается:** `[док]` — документация поставщика со ссылкой, `[замер]` — наш замер с датой и профилем, `[сторонний]` — независимое измерение, `[нет данных]`. Утверждение без пометки считается непроверенным.

**Дисквалификаторы первыми.** Жёсткие ограничения отсекают варианты без всякого сравнения. Обычно после них остаётся два-три кандидата, и взвешивать почти нечего.

**Баллы не суммируются.** Если хочется свести к числу — это признак, что не хватает дисквалификатора.

**Матрица — снимок, а не ссылка.** В ADR заполненная матрица копируется целиком, с датой. ADR фиксирует, что решили тогда и при каких условиях; ссылка на живой документ означает, что обоснование молча изменится вместе с ним. Ссылкой прикладывается только метод — сам документ решения и дата его рыночного снимка.

**Матрица объясняет себя.** В ней присутствуют **все** оси решения, и у каждой — колонка «что записывать в ячейку». Документ открывают те, кто видит его впервые; матрица из одних названий осей заполняется наугад или не заполняется вовсе. Ось, по которой варианты не различаются, вычёркивается; недостающая ось добавляется вместе со своим вопросом в раздел 1.

---

## Форма

```
# D-NN · <название решения>

Известно также как: <синонимы, как это называют в других командах и источниках>
Этап: <W-шаги, на которых принимается>
Профиль: <обратимость> · <влияние>
Карточки: <ссылки на topics/ за механикой>

## 1. Что выяснить
До сравнения вариантов. У каждого вопроса помечено, какую ось он заполняет.
5–7 вопросов. Если вопросов больше — решение слишком крупное, делится на два.

## 2. Оси и дисквалификаторы
Оси — то, по чему варианты различаются содержательно.
Дисквалификаторы — условия, при которых вариант выбывает без сравнения.

## 3. Матрица оценки
Строки — все оси, колонки — варианты. Отдельная колонка «что записывать
в ячейку». В ячейках факты с пометкой источника.
Заполняется в проекте; сюда попадает форма и условный пример — с оговоркой,
что числа в нём выдуманы, а проверенные факты живут в разделе 4.

## 4. Типовые решения · снято <дата>
Рынок на момент съёмки: ключевые факты со ссылками, жёсткие ограничения,
статус у нас (используем / не используем, почему / не рассматривали).
Раздел отсутствует, если решение не про выбор продукта.

## 5. Эвристики
Что делать при ничьей и при нехватке данных.

## 6. Запись в ADR
Какие поля заполнить. Матрица копируется снимком; ссылкой — только метод
и дата рыночного снимка. Правка этого документа не меняет принятых ADR:
изменившее бы решение изменение — это новый ADR, отменяющий старый.

## 7. Условия пересмотра
Что должно измениться, чтобы решение перестало быть верным.
```

## Проверка перед тем, как считать решение принятым

- [ ] Факты собраны, а не предположены — у каждой ячейки матрицы есть пометка источника
- [ ] Дисквалификаторы проверены до сравнения, а не после
- [ ] Рассмотрен вариант отложить решение, и записано, какие данные его снимут
- [ ] Записано, что изменит решение
- [ ] У отвергнутых вариантов записана причина — иначе их предложат заново

Пять пунктов одинаковы для всех решений и живут здесь, а не в каждом документе.

## Как это связано с остальным документом

Шаг workflow перечисляет решения, которые на нём принимаются, и ссылается сюда. Карточки в `topics/` объясняют механику и ссылаются на решение за выбором. Обратная ссылка обязательна: решение без входящей ссылки из шага недостижимо в работе.

---

**Источники метода:** [Atul Gawande — The Checklist Manifesto](https://us.macmillan.com/books/9780312430009/thechecklistmanifesto) (форма проверки и ограничение объёма) · [Michael Nygard — Documenting Architecture Decisions](https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions) (запись решения) · [`00-workflow.md`, шаг W10](../00-workflow.md#w10) (оси сравнения и обратимость)
